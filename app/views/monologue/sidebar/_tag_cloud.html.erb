<%= sidebar_section_for(t(".title")) do  %>
  <ul>
    <% @tags.each do |tag| %>
    <li> <%=link_to tag.name, tag_url(tag), class:label_for_tag(tag,@tags_frequency_min,@tags_frequency_max) %>
    </li>
    <% end %>
  </ul>
<% end  %>